{
  "private": true,
  "workspaces": [
    "example",
    "use-harperdb"
  ],
  "scripts": {
    "build": "yarn build:hook && yarn build:example",
    "build:example": "yarn workspace example package",
    "build:hook": "yarn workspace use-harperdb build",
    "bump": "yarn workspace use-harperdb bump",
    "develop": "yarn watch",
    "watch": "concurrently -n Hook,Example \"yarn watch:hook\" \"yarn watch:example\"",
    "watch:example": "yarn workspace example start",
    "watch:hook": "yarn workspace use-harperdb watch",
    "setup": "node ./scripts/setup.js"
  },
  "devDependencies": {
    "concurrently": "^5.1.0"
  }
}
